Supplier X:
  path: "supplier_x"
  controller: "supplier_x"
  workers:
    metadata:
      every: "1d"
    availabilities:
      every: "2h"

Supplier FOO:
  controller: "supplier_foo"
  workers:
    metadata:
      every: "3d"
    availabilities:
      every: "7h"
